name: android_video_converter
description: A video file converter app for Android
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  ffmpeg_kit_flutter: ^6.0.3
  file_picker: ^6.1.1
  path_provider: ^2.0.15
  permission_handler: ^10.4.3
  provider: ^6.0.5
  video_player: ^2.7.0
  flutter_riverpod: ^2.3.6
  go_router: ^10.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true